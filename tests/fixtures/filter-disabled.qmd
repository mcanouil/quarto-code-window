---
title: "The filter turned off"
filters:
  - code-window
extensions:
  code-window:
    enabled: false
---

A filter that is off leaves every block as Quarto writes it. The block below
keeps the language its author gave it, even though Pandoc cannot highlight it
and the filter would relabel it when it is on.

```foo
x = 1
```
