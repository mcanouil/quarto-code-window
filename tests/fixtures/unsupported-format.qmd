---
title: "A format the extension does not act on"
filters:
  - code-window
---

The filter draws no chrome outside HTML and Typst. Nothing reads a label here,
so the block below keeps the language its author wrote, and the filter's own
label for it is never written at all.

```foo
x = 1
```
